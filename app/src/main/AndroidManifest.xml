<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cook">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cook">
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".CatatanResep.updateData" />
        <activity android:name=".CatatanResep.mylist" />
        <activity android:name=".CatatanResep.CatatanResep" />
        <activity android:name=".Viewpager.ViewPageActivity" />
        <activity android:name=".ListKatagoriActivity" />
        <activity android:name=".PenjelasanDetail" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>